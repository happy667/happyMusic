export default class Album {
  constructor({
    id,
    name,
    picUrl,
    singerName,
    publishTime
  }) {
    this.id = id
    this.name = name
    this.picUrl = picUrl
    this.singerName = singerName
    this.publishTime = publishTime
  }
}
