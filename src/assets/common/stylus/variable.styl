// 颜色定义规范
$color-common=#FD4979
$color-common-x=#23203f
$color-common-b=#f4f4f4
$color-common-b2=#999
$color-common-background=#fff
// 字体定义规范
$font-size-mini-x = .25rem
$font-size-mini = .3rem
$font-size-smaller-x = .35rem
$font-size-smaller = .4rem
$font-size-small-x = .45rem
$font-size-small = .55rem
$font-size-large-x = .6rem
$font-size-large = .65rem
// 背景图片
$baImage=url('./assets/images/bgImages/bg1.jpg');
// 不换行
no-wrap()
  text-overflow: ellipsis
  overflow: hidden
  white-space: nowrap
  //超过两行换
no-wrap2(){
    display: -webkit-box;
    -webkit-line-clamp: 2;
    overflow: hidden;
    text-overflow: ellipsis;
    -webkit-box-orient: vertical;
    word-wrap: break-word;
}
  //超过三行换
no-wrap3(){
    display: -webkit-box;
    -webkit-line-clamp: 3;
    overflow: hidden;
    text-overflow: ellipsis;
    -webkit-box-orient: vertical;
}
roundImg(){
   display: block;
   width: 100%;
   height: 100%;
   border-radius: 50%;
}

// 扩展点击区域
extend-click()
  position: relative
  &:before
    content: ''
    position: absolute
    top: -10px
    left: -10px
    right: -10px
    bottom: -10px




 
